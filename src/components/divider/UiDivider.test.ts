import { mount } from '@vue/test-utils'; import UiDivider from './UiDivider.vue';
describe('UiDivider',()=>{it.each(['horizontal','vertical'] as const)('renders %s orientation',(orientation)=>{const w=mount(UiDivider,{props:{orientation,labelPosition:'end'},attrs:{title:'Break'}});expect(w.attributes('role')).toBe('separator');expect(w.attributes('aria-orientation')).toBe(orientation);expect(w.attributes('data-label-position')).toBe('end');expect(w.attributes('title')).toBe('Break');});it('renders optional label',()=>{expect(mount(UiDivider,{slots:{default:'Or'}}).get('.cui-divider__label').text()).toBe('Or');});it('omits empty label',()=>expect(mount(UiDivider).find('.cui-divider__label').exists()).toBe(false));});
