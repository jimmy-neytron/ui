import { mount } from '@vue/test-utils'; import UiEmptyState from './UiEmptyState.vue';
describe('UiEmptyState',()=>{it('renders props and size',()=>{const w=mount(UiEmptyState,{props:{title:'Empty',description:'Nothing here',size:'lg'}});expect(w.get('h3').text()).toBe('Empty');expect(w.get('p').text()).toBe('Nothing here');expect(w.attributes('data-size')).toBe('lg');});it('renders every slot',()=>{const w=mount(UiEmptyState,{slots:{icon:'I',title:'T',default:'D',actions:'A'}});expect(w.text()).toContain('ITDA');expect(w.get('.cui-empty-state__icon').attributes('aria-hidden')).toBe('true');});it('omits optional regions',()=>{const w=mount(UiEmptyState);expect(w.find('h3').exists()).toBe(false);expect(w.find('p').exists()).toBe(false);expect(w.find('.cui-empty-state__actions').exists()).toBe(false);});});
